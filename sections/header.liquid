<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <meta name="theme-color" content="#111111">

  <link rel="canonical" href="{{ canonical_url }}">

  {%- if settings.favicon != blank -%}
    <link
      rel="icon"
      type="image/png"
      href="{{ settings.favicon | image_url: width: 32, height: 32 }}"
      sizes="32x32">
  {%- endif -%}

  <title>
    {{ page_title }}
    {%- if current_tags -%} – {{ current_tags | join: ', ' }}{%- endif -%}
    {%- if current_page != 1 -%} – Page {{ current_page }}{%- endif -%}
    {%- if shop.name -%} – {{ shop.name }}{%- endif -%}
  </title>

  {%- comment -%} Helpful preconnects {%- endcomment -%}
  <link rel="preconnect" href="https://cdn.shopify.com" crossorigin>
  <link rel="preconnect" href="https://fonts.shopifycdn.com" crossorigin>

  {%- comment -%} Theme styles (keep base.css; add others if your theme has them) {%- endcomment -%}
  {{ 'base.css' | asset_url | stylesheet_tag }}

  {%- comment -%}
    REQUIRED: Shopify injects storefront scripts + app code here.
    Do not remove.
  {%- endcomment -%}
  {{ content_for_header }}
</head>
